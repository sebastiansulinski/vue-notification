import { mount } from '@vue/test-utils';
import { default as NotificationContainer } from '../../src/components/NotificationContainer';

describe('NotificationContainer test', () => {
  it('displays notification when one is available', () => {
    const wrapper = mount(NotificationContainer);
    // todo
  });
});
